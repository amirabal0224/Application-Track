# Application Tracker

Monorepo with:
- `backend/` FastAPI (Python)
- `frontend/` React (TypeScript)
- Postgres via Docker Compose

## Local dev
1. Start database:
- `docker compose up -d`

2. Start backend:
- See `backend/README.md`

3. Start frontend:
- See `frontend/README.md` (created after scaffolding)
